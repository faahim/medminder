# M2-001: App Lifecycle & Notification Setup

## Metadata
| Field | Value |
|-------|-------|
| Phase | 2 |
| Status | pending |
| Priority | P0 |
| Estimate | 30 min |
| Dependencies | Phase 1 Complete |
| Blocks | M2-002, M2-003 |
| Assigned | |
| Claimed At | |
| Completed At | |

## Objective
Integrate notification service into app lifecycle so notifications are scheduled, managed, and cleaned up reliably.

## Acceptance Criteria
- [ ] Request notification permissions on first app launch
- [ ] Reschedule all notifications on app startup
- [ ] Cancel notifications when medication is deleted
- [ ] Update notifications when medication is edited
- [ ] Clean up expired medication notifications
- [ ] Set up notification action categories (Take/Snooze/Skip)
- [ ] Handle timezone changes (reschedule all)
- [ ] Update badge count on app foreground

## Implementation Details

### App Startup (_layout.tsx)
```tsx
import { useEffect } from 'react';
import * as Notifications from 'expo-notifications';
import { NotificationService } from '../src/services/notification.service';

export default function RootLayout() {
  useEffect(() => {
    // Request permissions
    NotificationService.requestPermissions();

    // Setup categories
    NotificationService.setupNotificationCategories();

    // Reschedule all notifications
    // (Will load medications from DB and reschedule)

    // Set up notification listener
    const subscription = Notifications.addNotificationResponseReceivedListener(
      handleNotificationResponse
    );

    // Set up timezone change listener
    const timezoneListener = Notifications.addNotificationReceivedListener(
      handleTimezoneChange
    );

    return () => {
      subscription.remove();
      timezoneListener.remove();
    };
  }, []);
}
```

### Notification Response Handler
- Take Now: log dose, close notification, update UI
- Snooze: schedule snooze notification, close
- Skip: log as skipped, close notification

### Files to Modify
- `app/_layout.tsx` - Add lifecycle hooks
- `src/services/notification.service.ts` - May need minor tweaks
- `src/hooks/useDatabaseContext.tsx` - Add medication change listeners

## Context
Notification service exists but isn't wired into app lifecycle yet. This task makes it "live."
