# M1-002: Install & Configure New Dependencies

## Metadata
| Field | Value |
|-------|-------|
| Phase | 1 |
| Status | completed |
| Priority | P0 |
| Estimate | 20 min |
| Dependencies | M1-001 |
| Blocks | M1-003 |
| Assigned | |
| Claimed At | |
| Completed At | 2026-01-29T22:34:00Z |

## Objective
Install and configure the new dependencies needed for Editor's Choice level UI: SF Symbols, liquid glass effects, and ensure Reanimated is properly configured.

## Acceptance Criteria
- [x] Install `expo-symbols` for SF Symbols
- [x] Install `expo-glass-effect` for liquid glass (SDK 54 compatible)
- [x] Verify `react-native-reanimated` is properly configured
- [x] Verify `expo-haptics` is working
- [x] Update babel.config.js (added `react-native-reanimated/plugin` last)
- [x] Create test component to verify all new deps work (`app/dev/deps-test.tsx`)
- [x] Clean build passes (`npx expo export --platform ios`)

## Implementation Details

### Dependencies to Install
```bash
npx expo install expo-symbols
npx expo install expo-glass-effect
# Reanimated should already be installed
```

### Babel Config Check
Ensure reanimated plugin is in babel.config.js:
```javascript
module.exports = function (api) {
  api.cache(true);
  return {
    presets: ['babel-preset-expo'],
    plugins: ['react-native-reanimated/plugin'],
  };
};
```

### Test Component
Create `src/components/__tests__/DepsTest.tsx` to verify:
- SF Symbol renders
- Reanimated animation works
- Haptics trigger

## Context
- expo-symbols provides iOS SF Symbols (with fallback on Android)
- expo-glass-effect provides iOS 15+ blur effects
- These are critical for achieving native iOS look

## Execution Log
(To be filled during execution)

## Artifacts
(To be filled after completion)
