# M1-003: Native Tabs Migration

## Metadata
| Field | Value |
|-------|-------|
| Phase | 1 |
| Status | completed |
| Priority | P0 |
| Estimate | 40 min |
| Dependencies | M1-002 |
| Blocks | M1-010 |
| Assigned | |
| Claimed At | |
| Completed At | 2026-01-29T22:38:58Z |

## Objective
Replace the JavaScript-based tab bar with native iOS tabs using NativeTabs from expo-router. This gives us true platform-native feel with proper SF Symbols integration.

## Acceptance Criteria
- [x] iOS: Replace `<Tabs>` with `<NativeTabs>` in `app/(tabs)/_layout.tsx`
- [x] iOS: Use SF Symbols for all tab icons via `<Icon sf="..." />`
- [x] iOS: Icon selection matches spec (calendar, pills.fill, chart.bar.fill, gearshape.fill)
- [x] iOS: Tab bar tint color matches design (#06B6D4)
- [x] Android fallback: keep JS `<Tabs>` implementation so app remains cross-platform
- [x] No broken imports / TypeScript clean
- [x] Verified build: `npx tsc --noEmit` and `npx expo export --platform ios`

## Implementation Details

### New Tab Layout
```tsx
import { NativeTabs, Icon, Label } from 'expo-router/unstable-native-tabs';

export default function TabLayout() {
  return (
    <NativeTabs>
      <NativeTabs.Trigger name="index">
        <Icon sf="calendar" />
        <Label>Today</Label>
      </NativeTabs.Trigger>
      <NativeTabs.Trigger name="medications">
        <Icon sf="pills.fill" />
        <Label>Meds</Label>
      </NativeTabs.Trigger>
      <NativeTabs.Trigger name="history">
        <Icon sf="chart.bar.fill" />
        <Label>History</Label>
      </NativeTabs.Trigger>
      <NativeTabs.Trigger name="settings">
        <Icon sf="gearshape.fill" />
        <Label>Settings</Label>
      </NativeTabs.Trigger>
    </NativeTabs>
  );
}
```

### Files to Modify
- `app/(tabs)/_layout.tsx` - Complete rewrite
- `src/constants/layout.ts` - Remove TAB_BAR constants (native handles this)

### Files to Remove
- Any custom tab bar components

## Context
- NativeTabs is from `expo-router/unstable-native-tabs`
- This is iOS-specific; Android will need fallback
- See building-native-ui skill `references/tabs.md` for full guide

## Execution Log
- Updated `app/(tabs)/_layout.tsx` to render `NativeTabs` on iOS and keep JS `Tabs` on Android.
- Switched iOS tab icons to SF Symbols via `expo-router/unstable-native-tabs` `<Icon sf="..." />`.
- Verified: `npx tsc --noEmit`, `npx expo export --platform ios`.

## Artifacts
- `app/(tabs)/_layout.tsx` (iOS NativeTabs + Android fallback)
