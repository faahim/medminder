# M1-004: Core UI Components Rebuild

## Metadata
| Field | Value |
|-------|-------|
| Phase | 1 |
| Status | completed |
| Priority | P0 |
| Estimate | 60 min |
| Dependencies | M1-001 |
| Blocks | M1-005, M1-006, M1-007, M1-008 |
| Assigned | clawd |
| Claimed At | 2026-01-30T00:00:00Z |
| Completed At | 2026-01-30T00:00:00Z |

## Objective
Rebuild all core UI components with the new design system: modern shadows, continuous border curves, proper spacing, and animation-ready structure.

## Acceptance Criteria
- [x] Rebuild `Card` component using design tokens + modern feel
- [x] Rebuild `Button` component with variants (primary, secondary, ghost), sizing, press states
- [x] Rebuild `Input` component with consistent sizing + focus/error states
- [x] Rebuild `Typography` component using new type scale + tokens
- [x] Create `Icon` wrapper component (SF Symbols on iOS, fallback on Android/Web)
- [x] Rebuild `Badge` component for status indicators
- [x] Create `Pill` component for tags/chips
- [x] All components use design tokens from `src/design/tokens.ts`
- [x] Press interactions are animation-ready (Reanimated scale micro-interactions where appropriate)
- [x] No ad-hoc/legacy shadow props in components (use tokenized `boxShadow`)

## Implementation Details
### Files Modified/Added
- `src/components/ui/Card.tsx`
- `src/components/ui/Button.tsx`
- `src/components/ui/Input.tsx`
- `src/components/ui/Typography.tsx`
- `src/components/ui/Icon.tsx` (new)
- `src/components/ui/Badge.tsx` (new)
- `src/components/ui/Pill.tsx` (new)
- `src/components/ui/index.ts` (updated exports)

## Notes
- Kept `className` props for NativeWind compatibility, but core styling now comes from design tokens via inline styles.
- Used `boxShadow` tokens (not `elevation`/`shadowColor` inline) and `borderCurve: 'continuous'` for a calmer, more premium feel.
- Verified: `npx tsc --noEmit` and `npx expo export --platform ios`.

## Execution Log
- 2026-01-30: Rebuilt core primitives (Card/Button/Input/Typography) on tokens; added Icon/Badge/Pill; updated exports; verified build.

## Artifacts
- Build output: `dist/` (Expo export)
